# Changelog

## [Current]

### Added
- Documentación alineada a la API real de producción (`/api/v1`).
- Resumen de relaciones entre módulos y persistencia en PostgreSQL.
- Guías actualizadas para el frontend React.

### Changed
- Eliminadas referencias del stack antiguo.
- Actualizados los ejemplos de endpoints a los del backend real.

### Fixed
- Enmascaramiento de errores por `/error`.
- Configuración CORS incompatible con `allowCredentials(true)`.
- Proxy y base URL del frontend alineados con el flujo real.
